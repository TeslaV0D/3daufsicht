namespace _3DInteriorEditor.App.ViewModels;

/// <summary>
/// Blender-style orthographic-style camera presets (distance is adjusted by zoom routines).
/// </summary>
public enum StandardViewKind
{
    Top,
    Front,
    Right,
    HomePerspective,
}
