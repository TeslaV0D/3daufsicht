namespace _3DInteriorEditor.App.ViewModels;

/// <summary>
/// Active transform tool mode (drives gizmo in later phases).
/// </summary>
public enum TransformMode
{
    Translate,
    Rotate,
    Scale,
}
